[docker]
image = "iterare-llm:latest"

[session]
shell = "/bin/bash"

[claude]
credentials_path = "~/.iterare"

[firewall]
allowed_domains = [
    # Add custom domains here, e.g.:
    "pypi.org",
    "files.pythonhosted.org",
]
